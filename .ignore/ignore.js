// in case you're tooooo lazy to check all the permissions when generating a new github token, you can just use this code:

Array.from(document.getElementsByClassName("js-checkbox-scope parent-checkbox-scope")).forEach((i) => i.click())